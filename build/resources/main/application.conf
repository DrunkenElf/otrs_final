ktor {
  environment = production

  deployment {
    connectionGroupSize = 2
    workerGroupSize = 5
    callGroupSize = 10
    requestQueueLimit = 20
    runningLimit = 20
    autoreload = false
    port = 80
    watch = []
  }

  application {

    modules = [
      internship.ApplicationKt.module
    ]
  }
}